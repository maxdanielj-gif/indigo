// Image generation has been removed. This stub prevents import errors.
export const generateVisual = async (..._args: any[]): Promise<{ url: string }> => {
  throw new Error('Image generation is not available yet. Coming soon.');
};
